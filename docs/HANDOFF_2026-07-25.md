# Session Handoff — checkpoint 2026-07-25 (next step: POST-MARKET ANALYSIS)

Self-contained "start here" for the next session. The next task is a
**post-market analysis** of where the system now stands — enabled by two
things that just became true: the advisor's portfolio-risk layer is live,
and the advisor grading loop has produced its **first 21 graded calls**.
Companion docs (canonical detail): `docs/SESSION_HANDOFF.md` (running log),
`docs/VISION.md` (the "why"/gates), `docs/POST_MARKET_TODO.md` (open items),
`docs/UI_GODMODE_PLAN.md`, `docs/ADVISOR_MODULE.md`.

---

## 1. Where the project is, in one paragraph

Month-long **paper-trading** validation of an intraday auto-trader (brain =
`~/Desktop/GITHUB/zerodha-brain`, Python on Railway; dashboard =
`zerodha-trading`, Next.js on Vercel; data in Supabase prod
`gilmuwmtdpjccibfhqtx`). The engine runs full-day sessions capturing rich
decision/trade/candle data. **The strategy has no proven edge yet** — paper
PF ≈ 0.33 across the 3 measured days; that is expected and fine (VISION §7b:
"no edge found" is a valid outcome). The real edge verdict is **gate #6** (a
historical backtest), which is built but **blocked on Kite historical data**
(₹500/mo subscription — user's decision, see §6). Alongside the trader is a
**portfolio advisor** (daily HOLD/SELL guidance on the user's real, red
holdings) which just crossed two milestones (§4).

## 2. The three measured sessions (data-richness era)

| Date | Trades | W/L | P&L | Notes |
|---|---|---|---|---|
| 07-22 | 66 | 15/51 | −₹1,277.75 | first full post-gap session |
| 07-23 | 51 | 12/39 | −₹1,582.38 | SELL-fix + weekly-confluence went live; first BEARISH tape |
| 07-24 | 8 | — | −₹143.73 | **short** — late token, session started 14:34 IST, ~1h of market |

All sessions closed clean (MARKET_CLOSED, 0 stuck positions). Net: a
consistent small-losing paper strategy — the counterfactual audits confirm
no dark-flag tuning fixes a no-edge strategy.

## 3. What got built recently (all shipped + deployed)

**Advisor "god-mode" plan** (3 pillars of a world-class *decision* system):
- **Pillar 1 — probability calibration**: NOT built yet, was blocked on
  graded data → **now unblocked** (§4).
- **Pillar 2 — portfolio-level risk view**: ✅ SHIPPED (brain `f5e7858`) —
  `portfolio_risk()` adds single-name + sector concentration (correlation
  proxy) and **tax-loss-harvest** candidates (underwater names already
  flagged SELL/TRIM → realizable capital losses). Surfaced in Telegram
  digest + stored to `app_config.portfolio_risk_latest` + rendered on the
  `/advisor` page and command center.
- **Pillar 3 — regime-conditional factor weighting**: NOT built, was blocked
  on graded data → **now unblocked** (§4).
- **Weekly (higher-timeframe) confluence** (brain `cd92317`): `weekly_trend`
  + `daily_weekly_alignment` on every advice row (CONFLICT / ALIGNED_*),
  surfaced in reasons. **Dark-flagged** — logged + graded, NOT folded into
  the numeric score until evidence justifies (verified live: 20/20 rows
  carry it).
- **Grading loop**: `scripts/grade_advice.py` (standalone, on-demand) +
  `advisor_backtest.factor_attribution()` (per-factor hit-rate separation)
  + `get_evaluated_advice_with_features()`.

**UI god-mode** (`docs/UI_GODMODE_PLAN.md`, all done):
- `/insights`: **profit factor + max drawdown** tiles (the VISION §6.1
  go/no-go gate metrics) — live values PF 0.33, max DD 55.5R.
- `/trading`: **live RiskMeter** (`components/RiskMeter.tsx`) — floor / 3R
  stop / ceiling made visible.
- **Command-center landing page** (`app/page.tsx`, was a redirect) — one
  glance: paper engine + risk, portfolio health, advisor top action.

**Bug fixes this arc**: SELL decision-log orientation (brain `6948767` —
was corrupting Track C SHORT labels), candle-archive batch loss (earlier),
`label_decisions` full-day payload pagination (brain `420eec7`).

## 4. ⭐ TWO MILESTONES that make post-market analysis newly worthwhile

1. **Portfolio-risk is LIVE + verified.** 07-24's advisor run wrote
   `app_config.portfolio_risk_latest` (was empty before the deploy). So the
   concentration + tax-loss-harvest read now works end-to-end and shows on
   `/advisor` + the command center.
2. **First 21 advice calls are GRADED.** The 07-12 batch hit its
   10-trading-day MICRO horizon; the backtest pass judged them against what
   the stocks actually did. `graded_total = 21` official evaluated rows.
   **This unblocks the advisor learning loop** — `factor_attribution` and
   the track-record / calibration reads finally have real outcomes to chew
   on (they returned empty until now).

## 5. The deploy saga + the ONE open regression

- The brain sat on `cd92317` all of 07-23 because an **auto-deploy at 02:08
  FAILED**, leaving portfolio-risk (`f5e7858`) undeployed.
- Fixed 07-23 23:58 IST: I ran `railway up` from local `420eec7` (clean,
  `f5e7858` in history) — deployment `19b6ac0e` SUCCESS, clean startup,
  brain ONLINE. Verified 07-24 via `portfolio_risk_latest` writing. ✅
- Deploys here are **manual `railway up`** (service is NOT GitHub-connected).
  The Railway **MCP token is expired** ("Unauthorized"), but the **Railway
  CLI is authenticated** (`railway whoami` → singhakshayraj@ymail.com; linked
  project `stunning-harmony` / service `zerodha-brain` /
  service ID `8825ff1f-b184-4506-9c3b-b6b142118902`).
- 🔴 **OPEN REGRESSION — `git_sha = "unknown"`**: `railway up` uploads code
  but NOT the `.git` dir, so `config._resolve_git_sha()` falls through to
  `'unknown'` (it reads `RAILWAY_GIT_COMMIT_SHA`/`GIT_SHA` env, then local
  git). 07-24's decisions are stamped `"unknown"` instead of `420eec7` — a
  **traceability** gap (VISION REQ-020), not a functional bug (portfolio-risk
  etc. all run fine). **Fix (safe now — weekend, no session):** set a Railway
  env var so it survives tarball deploys:
  `cd ~/Desktop/GITHUB/zerodha-brain && railway variables --set "GIT_SHA=$(git rev-parse --short=12 HEAD)" --service zerodha-brain`
  (this restarts the brain — fine while market is closed; do NOT do it
  mid-session). Re-verify next session's `trading_sessions.git_sha` reads a
  real SHA.

## 6. Open items / what's next after the analysis

- **git_sha fix** (above) — quick, do post-market.
- **Advisor pillars 1 (calibration) + 3 (regime-conditional weights)** — NOW
  buildable (21 graded calls, growing). Run `scripts/grade_advice.py`
  (needs a venv w/ `requirements.txt`, and a live token for candle reads
  during grading) to accumulate more, then build calibration off the
  score-bucket → hit-rate mapping.
- **Weekly confluence → promote into score** once `factor_attribution` shows
  `daily_weekly_alignment` predicts.
- **Gate #6** — the real edge verdict. Harness (`backtest.py`) is built +
  tested. Blocked on **Kite Connect historical data** (₹500/mo; no static IP
  needed for data-only; decision recorded in VISION log). Plan: chunked
  puller → local Parquet → run `backtest.py`. This is the highest-value
  unstarted work.
- **Data-collection 3R-override cost**: measured — on both full days,
  continuing past the DAILY_STOP_3R marker bled extra (07-22 −803→−1,278,
  07-23 −888→−1,582). Revisit whether to keep overriding the stop once the
  data-volume goal is met.
- Full backlog: `docs/POST_MARKET_TODO.md`.

## 7. Key commands / locations

- Prod Supabase project: `gilmuwmtdpjccibfhqtx` (use `mcp__supabase__execute_sql`).
  Note timestamps are **UTC** (IST = UTC + 5:30) — don't misread session times.
- Post-session audits: `/post-session-check`, `/counterfactual-audit` skills.
- Advisor grading: `scripts/grade_advice.py` (brain) — `--attrib-only` needs
  no token. Pacing: `scripts/pacing_cost.py <dates>`. Label: `scripts/label_decisions.py <date>` (now handles full days).
- Brain repo `~/Desktop/GITHUB/zerodha-brain` on `main` @ `420eec7`; suite 807 green.
- Dashboard repo `~/Desktop/GITHUB/zerodha-trading` on `main`.

---

## 8. NEXT STEP — post-market analysis (what to actually do)

Run a proper post-market analysis now that the two milestones (§4) landed.
Concretely:
1. **Grade + attribution read**: run `scripts/grade_advice.py --attrib-only`
   (or query directly) to see the first `factor_attribution` output over the
   21 graded calls — which of the 7 advisor factors actually separated right
   from wrong calls, and whether `daily_weekly_alignment` shows early signal.
   Report the track record (hit rate, alpha, ₹ saved) too. **Caveat n=21:**
   label everything LOW-CONFIDENCE, do not act on it yet — this is a first
   look, not a mandate to reweight.
2. **Portfolio-risk read**: pull `app_config.portfolio_risk_latest` and
   sanity-check the concentration flags + tax-loss-harvest numbers against
   the real holdings — confirm they read correctly now that it's live.
3. **3-session synthesis**: PF/expectancy/drawdown trend across 07-22/23/24,
   the SIDEWAYS→BEARISH tape shift, and the standing conclusion (no edge yet
   → gate #6 is the priority).
4. Then do the **git_sha fix** (§5) as the one housekeeping item.

Weekend now (market closed) — all of this is read-only analysis except the
git_sha env fix. No live session to disturb.
