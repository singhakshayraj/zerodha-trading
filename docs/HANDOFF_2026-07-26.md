# Session Handoff — checkpoint 2026-07-26 (post-market analysis DONE)

Self-contained "start here". Supersedes `HANDOFF_2026-07-25.md`. The
2026-07-25 post-market analysis is **complete** (findings below). Next
priority is unchanged: **gate #6 backtest** (blocked on Kite data). Companion
docs: `docs/SESSION_HANDOFF.md`, `docs/VISION.md`, `docs/POST_MARKET_TODO.md`,
`docs/ADVISOR_MODULE.md`, `docs/UI_GODMODE_PLAN.md`.

Timestamps in Supabase prod `gilmuwmtdpjccibfhqtx` are **UTC** (IST = UTC+5:30).

---

## 1. One paragraph

Month-long **paper-trading** validation of an intraday auto-trader (brain =
`~/Desktop/GITHUB/zerodha-brain`, Python on Railway @ `420eec7`; dashboard =
`zerodha-trading`, Next.js on Vercel; data in Supabase `gilmuwmtdpjccibfhqtx`).
**Strategy has no proven edge** — paper PF deteriorating 0.39→0.33→0.22 across
the 3 measured days into a bearish tape (expected/fine, VISION §7b). Real edge
verdict = **gate #6** historical backtest, built but **blocked on Kite data**
(₹500/mo, user's decision). Alongside: a **portfolio advisor** (daily HOLD/SELL
on real holdings) — its portfolio-risk layer + grading loop both now verified.

## 2. ⭐ What the 2026-07-25 post-market analysis found

### 2a. Advisor grading — first 21 calls, **LOW-CONFIDENCE, DO NOT reweight**
- Track record: 21 calls (all 07-12/13 batch, 10d MICRO), **42.9% hit rate**,
  avg ret −2.45%, avg alpha −0.63%, **advice value +₹2,556** (exit calls saved
  money as flagged names fell).
- By verdict: HOLD 11@18.2% (down tape punished HOLDs), SELL 5@60% (+₹1,264),
  TRIM 4@100% (+₹1,508), SELL_ON_BOUNCE 1@0% (−₹217).
- Factor separation (n≥5 buckets): relative_strength 43.3pp, ema50 41.7pp,
  trend_consistency 21.4pp, ema200 13.6pp, adx 2.7pp (noise).
- 🚩 **Every separating factor is INVERTED** vs how the advisor weights it
  (right calls came from *weak* structure). NOT independent edge — n=21 all in
  one down-tape window + verdict entangled with factor (weak names got the
  SELL/TRIM verdicts that won because everything fell). Regime artifact →
  exactly what pillar-3 (regime-conditional weights) is for, but needs
  multi-regime data. **Weights untouched.**
- 🚩 **`daily_weekly_alignment` is NULL on all 21 rows** — feature shipped
  07-23 (`cd92317`), batch predates it. No early signal readable yet. First
  gradeable DWA calls mature ~**early August** (10 trading days past 07-23).

### 2b. Portfolio-risk layer (live 07-24) — VERIFIED reads correctly
- `total_value` ₹605,072 = Σ holdings mkt_val ₹605,073 (exact). top SILVERBEES
  14.8% ✓.
- `concentration_flags = []` is **CORRECT** (not a bug): top 14.8% < 25%
  single-name threshold; biggest real sector Capital Goods 15.2% < 35%
  threshold. Unknown 30.4% excluded by design (SILVERBEES ETF + unmapped).
- `tax_loss_harvest`: 9 names, all SELL/TRIM + underwater, Σ ₹64,080 =
  `harvestable_loss_inr` ✓. Correctly excludes 6 HOLDs + in-profit SELL/TRIMs
  (NMDC/NTPC/NBCC/BANKINDIA/HDFCSENSEX).
- Minor: 30.4% "Unknown" sector weakens sector-flag sensitivity —
  `sector_map` coverage gap to close later.
- Thresholds: `portfolio_advisor.py` CONCENTRATION_FLAG_PCT=25,
  SECTOR_CONCENTRATION_PCT=35.

### 2c. Three-session synthesis
| date | n | win% | net ₹ | PF | exp R | ΣR | git_sha |
|---|---|---|---|---|---|---|---|
| 07-22 | 66 | 22.7 | −1,277.75 | 0.387 | −0.44 | −29.01 | 78a8412 |
| 07-23 | 51 | 23.5 | −1,582.38 | 0.325 | −0.52 | −26.46 | cd923176 |
| 07-24 | 8 | 12.5 | −143.73 | 0.220 | −0.43 | −3.43 | unknown→fixed |

PF trending DOWN, expectancy stuck ~−0.5R, cumulative **−58.9R** (matches
/insights max-DD 55.5R). Tape SIDEWAYS(07-22)→BEARISH(07-23/24). **Standing
conclusion UNCHANGED: no proven edge (PF<1); dark-flag tuning can't fix a
no-edge strategy; gate #6 is the priority.**

### 2d. portfolio_risk v2 — measured return-correlation (brain `b919a84`, NOT deployed)
Built 2026-07-26 (the picked-up dev item). Replaces the sector proxy where
daily closes exist:
- `_correlation_read()` (pure): 60d daily-return correlation over holdings →
  **clusters** (connected components at corr≥0.7), **effective_bets** (1/wCw),
  **top_pairs**. Cluster ≥30% of book → appends a "move together — ~1 bet"
  concentration flag. New `correlation` block stored in `portfolio_risk_latest`.
- Kills the 30.4% "Unknown"-sector blind spot from §2b — clusters by real
  co-movement, not sector label (e.g. PSU-infra RVNL/NBCC/IREDA collapse to
  ~1 bet). Opt-in via `closes_by_symbol`; omit = v1 behaviour (backward compat).
  Closes collected in the existing holdings loop = zero extra fetches.
- Suite **811 green** (+4 tests). json.dumps + real-shaped smoke verified.
- ⚠ **NOT deployed** — needs a manual `railway up` (see §3). Won't run until
  the next live advisor run (needs enc_token). UI surfacing of the block on
  /advisor + command center is a follow-up (only `concentration_flags` renders
  today; the structured `correlation` block is stored but unshown).

## 3. git_sha regression — FIXED 2026-07-25
- Was: `railway up` uploads code but not `.git`, so `_resolve_git_sha()` fell
  to `'unknown'` (07-24 session stamped that).
- Fix: `railway variables --set "GIT_SHA=420eec75826d" --service zerodha-brain`
  (survives tarball deploys). Var confirmed present; set restarts brain — safe,
  market was closed.
- ✅ **Next session: verify `trading_sessions.git_sha` reads a real SHA.**
- ⚠ Must bump this var on every future `railway up` from a new SHA, else stale.

**Deploy portfolio_risk v2:**
1. ✅ DONE 2026-07-26 ~22:48 IST — `railway up` deployed main @ `b919a84`;
   new instance `704a5528` started clean (both advisor daemons up, heartbeat
   ONLINE, no errors; a transient telegram 409 dual-poller blip self-resolved).
2. ✅ DONE — `GIT_SHA=b919a849e639` set on the service (confirmed present);
   restart settled Online.
3. ⏳ Next session (user): paste enc_token before 09:15 IST (daily manual step)
   so the advisor run computes the correlation block.
4. ⏳ Verify next session: `app_config.portfolio_risk_latest` has a non-null
   `correlation` (`effective_bets`, `clusters`) AND `trading_sessions.git_sha`
   reads `b919a849e639`, not `unknown`.
- Railway: MCP token expired ("Unauthorized"); **CLI is authed**
  (singhakshayraj@ymail.com; project `stunning-harmony` / service
  `zerodha-brain` / id `8825ff1f-b184-4506-9c3b-b6b142118902`). Deploys manual
  `railway up` (not GitHub-connected).

## 4. Open items / next

- **Gate #6** (real edge verdict) — highest-value unstarted work. Harness
  `backtest.py` built+tested. Blocked on **Kite Connect historical data**
  (₹500/mo, data-only, no static IP). Plan: chunked puller → local Parquet →
  `backtest.py`.
- **Advisor pillars 1 (calibration) + 3 (regime weights)** — buildable once
  more graded calls accumulate + a second regime is captured. Keep running
  `scripts/grade_advice.py` (needs venv + live token for candle reads;
  `--attrib-only` needs no token).
- **Promote `daily_weekly_alignment` into score** — only once it's gradeable
  (~early Aug) AND `factor_attribution` shows real separation. Currently null.
- **Data-collection 3R-override cost** — measured bleed past DAILY_STOP_3R
  marker (07-22 −803→−1,278; 07-23 −888→−1,582). Revisit once data-volume goal
  met.
- **sector_map coverage** — reduce the 30.4% "Unknown" bucket.
- Full backlog: `docs/POST_MARKET_TODO.md`.

## 5. Key commands / locations
- Prod Supabase `gilmuwmtdpjccibfhqtx` (`mcp__supabase__execute_sql`).
  Timestamps UTC (IST = UTC+5:30).
- Audits: `/post-session-check`, `/counterfactual-audit` skills.
- Advisor grading: `scripts/grade_advice.py [--attrib-only]`. Pacing:
  `scripts/pacing_cost.py <dates>`. Label: `scripts/label_decisions.py <date>`.
- Advice table: `portfolio_advice` (is_official=true, evaluated_at not null =
  graded). Risk blob: `app_config.portfolio_risk_latest`.
- Brain `~/Desktop/GITHUB/zerodha-brain` main @ `420eec7`; suite 807 green.
  Dashboard `~/Desktop/GITHUB/zerodha-trading` main.
